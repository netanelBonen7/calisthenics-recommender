# Calisthenics Recommender

A calisthenics exercise recommendation project built with a clean architecture approach.

The MVP will recommend exercises using:
- structured user input
- deterministic hard filters
- embeddings-based semantic matching
- a pure Python core before API, database, Docker, or frontend layers

See `PROJECT_BRIEF_FOR_CODEX.md` for the source of truth.
