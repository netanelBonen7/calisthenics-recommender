[project]
name = "calisthenics-recommender"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = [
    "numpy>=2.4.4",
    "pydantic>=2",
    "sentence-transformers>=5.4.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.12",
]
